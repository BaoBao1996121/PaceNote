# Bug 模式分析

> 🤖 **AI 生成文件** — 由 `#bug-diagnosis-expert` Mode A 自动生成。
>
> 基于历史 Bug 数据 + PR 代码变更，由 LLM 分析总结。

⚠️ **尚未生成** — 请运行以下命令收集数据后，使用 `#bug-diagnosis-expert` Mode A 生成：

```bash
python scripts/analyze_bug_prs.py --module <模块名>
```
